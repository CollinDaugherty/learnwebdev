{
  "scripts": {
    "start": "cd client && npm start && cd ../server && node index.js",
    "heroku-postbuild": "cd client && npm install && npm run-script build && cd ../server && npm install"
  }
}
